# .env.production
NODE_ENV = production

# 生产环境API地址（使用相对路径，通过Nginx代理）
VITE_API_URL = /api

# 端口号
VITE_PORT = 80

# 是否开启CDN
VITE_CDN = false

# 是否开启压缩
VITE_COMPRESSION = gzip

# 公共路径
VITE_PUBLIC_PATH = /

# 路由历史模式（hash或h5）
VITE_ROUTER_HISTORY = "hash"